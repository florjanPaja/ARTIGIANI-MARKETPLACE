# Database Marketplace - Artigianato Online

## 📦 Struttura

- `init.sql`: Crea tutte le tabelle relazionali e le relazioni.
- `seed.sql`: Popola il database con dati di esempio.
- `schema.png`: Schema ER grafico del database.

## 🛠️ Setup Manuale

Per avviare il database:

```bash
docker compose up -d db
